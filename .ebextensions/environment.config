files:
    "/etc/php.d/99uploadsize.ini":
        mode: "000644"
        owner: root
        group: root
        content: |
            upload_max_filesize = 100M
            post_max_size = 100M
container_commands:
    01-migration:
        command: "php artisan migrate --force"
        leader_only: true
